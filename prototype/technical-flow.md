# 蜂汇智理 技术流程与架构说明（原型 v0.1）

- 负责人：AI 3
- 所属轮次：第一轮
- 目标：说明原型如何模拟「上报 → AI 处理 → 建议分派 → 人工决定 → 反馈 → 激励」的完整链路

---

## 1 架构总览

```text
┌──────────────────────────── 浏览器（离线可运行）────────────────────────────┐
│  index.html   结构壳 + 顶部「原型演示环境」声明条 + 演示控制台              │
│  styles.css   设计系统（骑手端移动壳 / 治理端后台 / 指标卡 / 徽标）        │
│  data.js      虚构与脱敏测试数据、AI 规则表、占位仿真结果                  │
│  engine.js    规则式「AI 处理管线」+ 指标口径计算（纯函数）                │
│  app.js       路由、状态存储（localStorage）、界面渲染、人工动作处理       │
│  shot.js      无头截图脚本（Node + Edge，仅开发期使用）                    │
└───────────────────────────────────────────────────────────────────────────┘
```

设计取舍：

| 取舍 | 原因 |
| --- | --- |
| 零依赖、零构建、纯静态 | 保证任何评委电脑上双击即可运行，录屏不会因环境失败 |
| 规则式 AI 而非真实模型调用 | 演示需要确定性；且不引入外部 API 与密钥 |
| 数据存 `localStorage` | 刷新不丢状态，便于分段录屏 |
| 地图为 SVG 示意底图 + 虚构网格 | 避免使用真实行政区划数据与未审核小区地图 |

## 2 数据模型

### 2.1 工单（Ticket）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 工单编号，如 `T-20260312-07` |
| `createdAt` | string | 演示时间戳（不取系统真实时间，保证可重复） |
| `channel` | enum | `voice` / `text` / `photo` / `location` / `mixed` |
| `rawText` | string | 上报人原始描述（含未脱敏内容，仅本地演示） |
| `desensitized` | object | 脱敏结果：`text`、`removed[]` |
| `structured` | object | 结构化要素：`what`、`where`、`when`、`who`、`impact` |
| `category` | object | `key`、`name`、`confidence`、`reason`、`secondary[]` |
| `risk` | object | `level`（`low`/`medium`/`high`/`emergency`）、`reason` |
| `duplicateOf` | string\|null | 重复合并候选 |
| `suggestDispatch` | object | `level`（社区/街道/市级）、`owner`、`sla`、`reason` |
| `decision` | object\|null | 人工决定：`action`、`owner`、`note`、`decidedAt` |
| `status` | enum | 见 `user-flow.md` 第 2.3 节 |
| `timeline` | array | 事件节点：`{at, actor, text, kind}` |
| `resolution` | object\|null | `text`、`points`、`at` |
| `emergency` | boolean | 是否紧急分流 |
| `modifiedByReporter` | boolean | 上报人是否修改过 AI 建议 |

### 2.2 服务点、激励与情景

| 实体 | 关键字段 |
| --- | --- |
| 服务点 `ServicePoint` | `id`、`name`、`type`、`services[]`、`openHours`、`note`（均为虚构演示数据） |
| 激励 `IncentiveRule` | `id`、`name`、`condition`、`points`、`benefit` |
| 情景 `Scenario` | `id`（S0–S3）、`name`、`added`、`mechanics[]` |
| 指标 `Metric` | `key`、`name`、`unit`、`direction`、`note` |

## 3 AI 处理管线（原型实现）

管线在 `engine.js` 中实现为 6 个纯函数，与确认页的区块一一对应：

```text
① desensitize(text)        脱敏
② structure(text, ctx)     结构化
③ classify(text)           类别建议 + 置信度
④ detectDuplicate(ticket)  重复检测与合并建议
⑤ assessRisk(text, cat)    风险分级 + 紧急判定
⑥ suggestDispatch(cat,risk) 建议分派（社区 / 街道 / 市级）+ 时限
```

### 3.1 脱敏规则（可解释）

| 规则 ID | 目标 | 处理方式（与 `engine.js` 实现一致） | 实现状态 |
| --- | --- | --- | --- |
| `PII-ID` | 身份证号 | 整段遮蔽，保留前 6 后 3，末尾标注「（已移除）」；**优先于手机号规则执行**，否则 18 位中的 11 位数字会被手机号规则误伤 | 已实现 |
| `PII-PHONE` | 手机号 | 保留前三后二，形如 `138****00`；使用数字边界避免匹配长数字串内部 | 已实现 |
| `PII-PLATE` | 车牌 | 保留省份简称与首位字母，形如 `沪A·***` | 已实现 |
| `PII-CONTACT` | 微信号 / QQ | 替换为「联系方式已隐藏」 | 已实现 |
| `PII-ADDR` | 精确门牌号 | 保留到楼栋，户号标注「已降精度」 | 已实现 |
| `PII-NAME` | 疑似真实姓名 | 替换为「某先生 / 某女士」 | 已实现（演示数据中未触发） |
| `PII-FACE` | 人脸 | 部署阶段规则：真实照片进入 AI 前在本地打码或模糊 | **未实现**（原型使用内置演示图，不处理真实照片） |

> 第二轮口径修正（对应 `gap-list-v1.md` T-15）：此前文档写作 `PII-WECHAT`、身份证「整段移除」、并列出不存在的 `SAFE-KEEP` 规则，现已按代码实际行为更正。位置与时间不参与脱敏，但也不再登记为脱敏规则。

每次脱敏都会返回 `removed[]` 明细，界面上以「已脱敏」标签逐条展示，演示时可解释。

### 3.2 分类规则表（节选）

| 类别 | 命中关键词示例 | 默认风险 | 建议层级 |
| --- | --- | --- | --- |
| 市容环境 | 垃圾、堆物、污水、油污、小广告 | low | 社区 |
| 道路与设施 | 井盖、路面、路灯、护栏、积水、塌陷 | medium | 街道 |
| 交通与秩序 | 违停、占道、单车、施工围挡 | low | 街道 |
| 公共安全 | 消防通道、燃气、电线、裸露 | high | 街道 / 市级 |
| 社区服务 | 驿站、取餐点、无障碍、休息区 | low | 社区 |
| 紧急事件 | 火灾、打架、伤者、交通事故、燃气泄漏 | emergency | 法定应急渠道 |

分类结果包含置信度与命中理由，便于解释「AI 为什么这样判」。

### 3.3 重复合并规则

```text
同一类别 且 网格距离 ≤ 1 格 且 48 小时内  → 建议合并
网格距离 ≤ 1 格 且 类别不同            → 提示「可能相关」
```

合并建议只作为**建议**出现在确认页；上报人可以选择合并或继续单独上报，治理端仍保留人工判断。

### 3.4 风险分级

| 等级 | 判定 | 处理路径 |
| --- | --- | --- |
| 紧急 | 命中紧急词表 | 退出普通工单流，展示应急转接卡 |
| 高 | 消防、燃气、电力、结构安全类关键词 | 高风险标记，建议 24 小时内处置，需人工确认 |
| 中 | 道路设施、积水、井盖等 | 建议 72 小时 |
| 低 | 市容、秩序、服务类 | 建议 7 天 |

### 3.5 建议分派

建议分派由「类别 × 风险 × 位置层级」共同决定，输出：`层级`、`建议处置主体`（虚构示例）、`建议时限`、`理由`。
输出文案固定为「AI 建议」，治理端必须人工点击确认才生成决定。

## 4 人工决定与反馈链路

```text
确认并分派 ──► decision{action:'dispatch', owner, note} ──► status='dispatched'
退回补充   ──► decision{action:'need_info', note}        ──► status='need_info'
转交       ──► decision{action:'transfer', owner}       ──► status='transferred'
录入处置   ──► resolution{text, points}                 ──► status='resolved' + 积分入账
```

每次动作都会写入 `timeline`，骑手端进度页按时间轴展示，实现「透明反馈」机制（S2）。

## 5 指标口径

指标口径分两层：**原型演示口径**由 `engine.js` 计算（基于内置示例工单）；**实验口径**来自 AI 2 的仿真，报告与视频只引用后者。

### 5.1 实验口径（报告与界面使用，来源：`simulation/experiment-plan.md`）

| 指标（界面名称） | 口径（逐字来自仿真实验计划） | 单位 | 方向 |
| --- | --- | --- | --- |
| 有效上报率 | 有效独立上报及成功合并佐证数 ÷ 普通原始上报数 | % | 越高越好 |
| 重复或无效上报率 | 无效上报及未合并重复数 ÷ 普通原始上报数（与有效上报率互补，和为 100%） | % | 越低越好 |
| 工单分派准确率 | 正确分派工单数 ÷ 工单数 | % | 越高越好 |
| 平均处置时间 | 已解决工单的处置小时均值 | 小时 | 越低越好 |
| 平均上报耗时 | 所有实际上报（含应急转接）的分钟均值 | 分钟 | 越低越好 |
| 群体参与差距 | 骑手参与率与网约车司机参与率之差的绝对值 | 百分点 | 越小越好 |

两处必须随数值一起说明的口径限制：

1. **框架指标名与实测范围不一致**：冻结底稿的「参与者时间成本」包含操作与等待两部分，本轮实测只覆盖上报环节，因此界面与报告统一表述为**「平均上报耗时」**并加脚注。
2. **「群体参与差距」不是多群体极差**：本轮仿真只有骑手与网约车司机两类群体，因此该指标是两类之差；而且它是**每次运行下两组参与率之差的均值**，不等于两组均值的差（S0 为 1.19 个百分点，而两组均值为 12.26% 与 11.93%）。两者不可互相推算。

### 5.2 原型演示口径（仅用于说明指标如何计算）

| 指标 | 原型内的计算方式 | 可否引用 |
| --- | --- | --- |
| 有效上报率 | （总上报 − 疑似重复）/ 总上报 | 只引用口径，不引用数值 |
| 重复或无效上报率 | 疑似重复工单占比（**未含无效上报**） | 只引用口径，不引用数值 |
| 工单分派准确率 | 已分派工单中未被退回的比例（用备注文本判定，已标注为弱口径） | 只引用口径，不引用数值 |
| 平均处置时间 | 示例工单上报到办结的平均时长 | 只引用口径，不引用数值 |
| 平均上报耗时 | **不在原型内计算**，由仿真侧给出，界面显示「—」并注明来源 | — |
| 群体参与差距 | **不在原型内计算**，由仿真侧给出，界面显示「—」并注明来源 | — |

原型示例工单只有 8 条，比例类指标容易得到失真的极端值（例如演示数据下分派准确率为 100%），因此**报告与视频不引用原型内的比例数值**，只引用其口径说明。

## 6 与 AI 2 结果的对接（第二轮已执行）

### 6.1 已完成的替换（2026-09-15）

| 步骤 | 执行情况 |
| --- | --- |
| ① 从仿真结果提取数值 | 已从 `simulation/results/aggregated-metrics.csv`（均值与 95% 区间）、`paired-effects.csv`（配对差）、`run-metrics.csv`（分群体参与率，20 次均值）提取 |
| ② 移除占位前缀 | 已删除全部 `PLACEHOLDER_SIM_RESULT` 数值与前缀，改为 `dataSource: "AI2-simulation"` |
| ③ 同步数据层 | `prototype/test-data/sim-results.json` 与 `app/data.js` 的 `SIM_RESULTS` 已同步 |
| ④ 重新生成截图 | `node prototype/app/shot.js` 已重出 14 张截图 |
| ⑤ 更新演示脚本 | `demo-script.md` 镜 8 旁白已改为真实数值与限定语 |

### 6.2 替换时的口径修正（不可省略）

| 修正 | 原因 |
| --- | --- |
| 参与差距由「四分组极差」改为「骑手与司机两类之差」 | 仿真只有两类群体数据；原四分组图无数据支撑 |
| 参与差距改为如实呈现扩大趋势（1.19 → 2.33 → 2.81） | 实测方向与早期占位趋势相反，必须以保存结果为准 |
| 「参与者时间成本」改为「平均上报耗时」 | 实测范围只覆盖上报环节 |
| 每格数值下方增加 95% 区间 | 报告要求点估计与区间成对出现 |
| 界面与文档全面禁用「显著」 | 本轮未做任何显著性检验 |
| 补充分组口径警示 | 差距指标是逐次差值的均值，与两组均值之差不同 |

### 6.3 当前状态与仍缺材料

| 项目 | 状态 |
| --- | --- |
| 六项主指标 S0–S3 数值与区间 | **已就位**（AI 2 第一轮结果） |
| 递进机制配对效应 | **已就位** |
| 分群体参与率（两类） | **已就位** |
| 关键参数敏感性分析 | **缺** → 界面标注 `SIM_RESULT_NEEDED_SENSITIVITY`，不以估计值代替 |
| 第一轮最终汇总 | **缺**（`simulation/results/final-summary.md` 不存在） → 标注 `SIM_RESULT_NEEDED_FINAL_SUMMARY` |
| 玉兰万象平台复跑 | **缺** → 标注 `SIM_RESULT_NEEDED_ONESIM_RERUN` |

上述三项缺失在仿真端页面以「尚未产出的仿真材料」区块显式列出，并在自检脚本中断言其保留 `SIM_RESULT_NEEDED` 标记。

## 7 安全与合规在技术上的落点

| 要求 | 技术实现 |
| --- | --- |
| 无密钥、无外部请求 | 全部资源本地；页面不发起任何网络请求（截图脚本除外，仅读取本地文件） |
| 无真实个人信息 | 测试数据虚构；`prototype/test-data/*.json` 中不含真实姓名、手机号、车牌 |
| 紧急事件不自动化 | 紧急工单不参与普通分派建议，提交后进入专用「已留存线索」页，不进入普通进度视图 |
| 人工决定不可绕过 | 治理端无自动确认代码路径；`decision` 只能由用户点击产生 |
| 待补材料不混淆 | 未产出的敏感性分析与平台复跑以 `SIM_RESULT_NEEDED_*` 标注，界面明确声明「不使用估计值代替」 |
| 数据用途与单独同意 | 上报页提供「数据用途与单独同意」入口与三项勾选（含位置信息单独同意），见 `safety-and-privacy.md` 第 3.3 节 |
| AI 生成内容标识 | 确认页标注 `AI 生成` 徽标，说明摘要、类别建议与图像标签由 AI 生成 |
| 申诉与举报入口 | 上报页与确认页提供申诉入口占位，说明正式上线需给出受理主体与反馈时限 |
| 声明常驻 | 顶部声明条在所有屏幕可见；演示模式只隐藏控制台，不再隐藏声明条 |

## 8 已知技术局限

- 规则式分类在跨类别、长文本、方言口语场景下会退化，仅用于演示可解释流程。
- 无真实地理编码：位置来自虚构网格，距离计算为网格距离近似。
- 无后端：多人协作、并发冲突、权限控制均未实现，属演示范围外。
- 未实现上报人「退回补充」的回填表单（治理端可退回，上报人侧只显示待补充事项）。
- 未做持久化加密；`localStorage` 中的演示数据可被浏览器清除。
- 办结产生的积分只写入时间轴与结果卡，不累计到账户余额（无积分台账与兑换动作）。
- 脱敏规则使用后行断言（lookbehind），需 Chrome 62+ / Edge 79+；旧内核浏览器会直接报错。
- 「合并重复」目前只给出重复候选与建议文案，没有合并动作按钮（保留为第二轮候选）。
- 地图与治理端列表共用同一份演示工单，刷新后选中状态会回到第一条。
